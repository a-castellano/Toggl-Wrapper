image: registry.windmaker.net:5005/a-castellano/limani/base_perl_utils:202104052102

stages:
    - test


tests:
  stage: test
  before_script:
    - sudo apt-get update
    - sudo apt-get -y install libdatetime-format-iso8601-perl libemail-abstract-perl libnet-http-perl libjson-types-perl libcpanel-json-xs-perl libio-all-lwp-perl libmoose-perl libmoosex-semiaffordanceaccessor-perl libmoosex-strictconstructor-perl libmoosex-types-common-perl libconfig-json-perl libjson-any-perl libjson-perl libdate-calc-perl libboolean-perl libmoosex-types-email-perl libjson-parse-perl
    - sudo apt-get install -y libtest-more-utf8-perl libtest-class-perl libtest-mockmodule-perl
    - perl Makefile.L
    - make
    - mutator mutate lib/Toggl/Wrapper.pm
    - mutator mutate lib/Toggl/Wrapper/TimeEntry.pm
    - cpanm Devel::Mutator
  script:
    - make test
    - mutator test

